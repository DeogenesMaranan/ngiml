[pytest]
testpaths = tests
norecursedirs = .git .venv .pytest_cache temp datasets prepared prepared_test_datasets test_datasets
addopts = -p no:tmpdir -p no:cacheprovider
cache_dir = .pytest_cache_local
